export const MAX_VISIBLE_COURSES = 4
export const MAX_VISIBLE_LECTURERS = 5

export const CAROUSEL_CARD_STYLE = {
  flex: '0 0 calc((100% - (var(--carousel-cols) - 1) * var(--carousel-gap)) / var(--carousel-cols))',
} as const
